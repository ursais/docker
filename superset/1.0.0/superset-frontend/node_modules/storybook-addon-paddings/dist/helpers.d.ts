import { WrapperSettings } from '@storybook/addons';
declare type Option = {
    name: string;
    value: string;
    default?: boolean;
};
declare type Options = Option[] | WrapperSettings['parameters'];
export declare const isEnabled: (options: Options) => options is Option[];
export declare const getSelectedPadding: (options: Options, currentValue: string) => string;
export {};
